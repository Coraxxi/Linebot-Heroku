# 08-2 測試API-新增學生資料

```
   |__ index.js      
   |
   |__ <utility>
           |__ asyncDB.js
           |__ student.js        
```

## 執行方式
```
(1) 在命令提示字元畫面中, 進入index.js在資料夾
(2) npm install pg --save
(3) node index.js
```

## 1. asyncDB.js 
```
填入自己的pgConn
```
#### 程式
``` js
'use strict';

//-----------------------
// 引用資料庫模組
//-----------------------
const {Client} = require('pg');

//-----------------------
// 自己的資料庫連結位址
//-----------------------
var pgConn = 'postgres://自己的資料庫連結URI';


//產生可同步執行query物件的函式
function query(sql, value=null) {
    return new Promise((resolve, reject) => {
        //設定資料庫連線物件
        var client = new Client({
            connectionString: pgConn,
            ssl: true
        })     

        //連結資料庫
        client.connect();

        //回覆查詢結果  
        client.query(sql, value, (err, results) => {                   
            if (err){
                reject(err);
            }else{
                resolve(results);
            }

            //關閉連線
            client.end();
        });
    });
}

//匯出
module.exports = query;
```


## 2. student.js 
```
這個程式提供以下API讓前端呼叫:

1. 新增學生資料
   (1)addStudent(stuNo, stuName, gender) 
   (2)傳入值: 學號(string), 姓名(string), 性別(string)
   (3)回傳值:
        |__ -9(執行錯誤)
        |__ n(新增筆數)        
```
#### 程式
``` js
'use strict';

//引用操作資料庫的物件
const query = require('./asyncDB');

//------------------------------------------
// 新增學生資料
//------------------------------------------
var addStudent = async function(stuNo, stuname, gender){
    //存放結果
    let result;  

    //讀取資料庫
    await query('insert into student (stuno, stuname, gender) values ($1, $2, $3)', [stuNo, stuname, gender])
        .then((data) => {
            result = data.rowCount;  //新增資料數 
        }, (error) => {
            result = -9;  //執行錯誤
        });

    //回傳執行結果
    return result;  
}
//------------------------------------------

//匯出
module.exports = {addStudent};
```

## 3. index.js

``` js
//增加引用函式
const student = require('./utility/student');

student.addStudent('130001' ,'王小明', 'M').then(data => {  
    if(data == -9){                    
        console.log('執行錯誤');
    }else{
        console.log('已增加' + data + '筆記錄');
    }  
})
```
